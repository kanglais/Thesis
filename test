










from collections import Counter

data = []
top_hashtags = []
top_mentions = []

new_data = {}

for doc in collection.find({'user.screen_name': 'theblaze'}):
    
    for item in doc:
        
        username = doc['user']['screen_name']
        total_tweets = doc['user']['statuses_count']
        
        hashtags = doc['entities']['hashtags']
        mentions = doc['entities']['user_mentions']
        
    for tag in hashtags:             
        text = tag['text']
        top_hashtags.append(text)
        
    for mention in mentions:
        mention_sn = mention['screen_name']     
        top_mentions.append(mention_sn)

    #influence = normalized_number

    #standard_dev = [len(top_hashtags), total_tweets]
    #print(standard_dev) 
    #influence = numpy.std(standard_dev)
    #don't need to compute :D 

    #new_data.update({'username': username, 'total_tweets': total_tweets, 'top_hashtags': top_hashtags, 'top_mentions': top_mentions})

print(new_data)
    
#top_h = Counter(top_hashtags)
#print(top_h.most_common(10))

#top_m = Counter(top_mentions)
#print(top_m.most_common(10))
    #print(influence)

print(username)
#print(total_tweets)


.dtype(float32)

